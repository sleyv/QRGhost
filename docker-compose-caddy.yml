# If you are already running Caddy on your server, you can simply add these lines
# to your existing Caddyfile instead of running this container:
#
# qrghost.your.domain {
#     root * /path/to/qrghost/public
#     file_server
# }
#

services:
  qrghost:
    image: caddy:alpine
    container_name: qrghost
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./public/index.html:/srv/index.html:ro
      - ./public/manifest.json:/srv/manifest.json:ro
      - ./public/sw.js:/srv/sw.js:ro
      - ./public/icon-192.png:/srv/icon-192.png:ro
      - ./public/icon-512.png:/srv/icon-512.png:ro
      - caddy_data:/data
      - caddy_config:/config
    command: caddy file-server --domain qrghost.your.domain

volumes:
  caddy_data:
  caddy_config:
